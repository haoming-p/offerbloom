const PracticeSection = () => {
  return (
    <section
      id="practice"
      className="h-screen snap-start px-16 py-24 bg-white flex items-center"
    >
      {/* TODO: Strength 3 — practice, record, track, get feedback */}
      <div className="max-w-7xl mx-auto w-full text-gray-400">
        Strength 3: Practice + track + feedback — coming soon
      </div>
    </section>
  );
};

export default PracticeSection;
