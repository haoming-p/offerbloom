const FAQSection = () => {
  return (
    <section
      id="faq-section"
      className="h-screen snap-start px-16 py-24 bg-white flex items-center"
    >
      {/* TODO: FAQ — port existing FAQ items from old HelloPage */}
      <div className="max-w-7xl mx-auto w-full text-gray-400">
        FAQ — coming soon
      </div>
    </section>
  );
};

export default FAQSection;
