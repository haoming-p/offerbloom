const TechStackSection = () => {
  return (
    <section
      id="tech-stack"
      className="h-screen snap-start px-16 py-24 bg-white flex items-center"
    >
      {/* TODO: How we built it — Neo4j, FastAPI, Claude, R2, Vercel/Railway */}
      <div className="max-w-7xl mx-auto w-full text-gray-400">
        How we built it — coming soon
      </div>
    </section>
  );
};

export default TechStackSection;
