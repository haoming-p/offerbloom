const MultiRoleSection = () => {
  return (
    <section
      id="multi-role-section"
      className="h-screen snap-start px-16 py-24 bg-white flex items-center"
    >
      {/* TODO: Strength 1 — multi-role prep (PM + SWE in one place) */}
      <div className="max-w-7xl mx-auto w-full text-gray-400">
        Strength 1: Multi-role prep — coming soon
      </div>
    </section>
  );
};

export default MultiRoleSection;
